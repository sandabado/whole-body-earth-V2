export function ProductionTicker({ items }: { items: string[] }) { const line = items.map((item) => `● ${item}`).join("   "); return <section aria-label="Now in production" className="overflow-hidden border-y border-water/40 bg-water/10 py-4"><div className="animate-marquee whitespace-nowrap font-mono text-xs uppercase tracking-[0.13em] text-water"><span>{line}&nbsp;&nbsp;&nbsp;</span><span aria-hidden="true">{line}&nbsp;&nbsp;&nbsp;</span></div></section>; }
