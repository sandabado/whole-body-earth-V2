import { PressSectionHeading } from "@/components/press/PressSectionHeading";
import { PressSubmissionForm } from "@/components/press/PressSubmissionForm";

export default function SubmissionsPage() { return <div className="px-6 py-14 md:py-20"><div className="mx-auto max-w-[850px]"><PressSectionHeading eyebrow="Author submissions" title="If your work serves the Living Earth, we want to read it."><p>We publish aligned authors. Not trends. Not algorithms.</p></PressSectionHeading><div className="mt-10 grid gap-7 border-y border-press/30 py-8 sm:grid-cols-3"><Promise title="100% IP">Authors retain copyright, distribution rights, and intellectual property.</Promise><Promise title="50/50 royalty">A clear split that values the author and the work equally.</Promise><Promise title="30 days">Every proposal receives a response within 30 days.</Promise></div><div className="mt-12"><PressSubmissionForm /></div></div></div>; }
function Promise({ title, children }: { title: string; children: React.ReactNode }) { return <div><p className="font-mono text-xs uppercase tracking-[0.14em] text-press">{title}</p><p className="mt-2 text-sm leading-relaxed text-ghost">{children}</p></div>; }
