import Link from "next/link";
import { GuardianSection } from "@/components/guardian/GuardianSection";
const code = [["The whole over the self", "We serve the Constellation before our own venture. Profit without contribution is extraction."], ["Transparency without exposure", "We share financials with auditors and systems with partners. Boundaries protect the work."], ["Feed First economics", "The artist, author, and builder eat first. Then the treasury. Then infrastructure."], ["Audit as practice", "Annual audits are accountability — the covenant we submit to willingly."], ["Revenue as responsibility", "$50,000/year is the floor for survival with dignity. Responsibility scales with revenue."], ["Referral as curation", "We do not accept cold applications. Slow and deliberate protects quality."], ["Succession as obligation", "No one owns the Constellation. Document, train, and transfer what must outlast us."]];
export default function ManifestoPage() { return <main className="px-6 py-16 md:py-24"><div className="mx-auto max-w-[900px]"><GuardianSection eyebrow="The Guardian Manifesto" title="This is the code."><p>Read it before you apply. If it resonates, you may belong here. If it repels, you are safe elsewhere.</p></GuardianSection><div className="mt-14 divide-y divide-guardian/30 border-y border-guardian/30">{code.map(([title, body], index) => <article key={title} className="py-8"><p className="font-mono text-xs text-guardian">0{index + 1}</p><h2 className="mt-2 font-display text-2xl font-bold">{title}</h2><p className="mt-4 max-w-2xl leading-relaxed text-ghost">{body}</p></article>)}</div><Link href="/pillars/guardian" className="mt-12 inline-block font-mono text-xs uppercase tracking-[0.13em] text-guardian">Return to the gate →</Link></div></main>; }
