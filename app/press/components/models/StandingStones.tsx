"use client";
import { useRef } from "react";import { useFrame } from "@react-three/fiber";import { Edges } from "@react-three/drei";import type { MeshStandardMaterial } from "three";import { STONES } from "./cathedral-data";
export default function StandingStones(){const caps=useRef<MeshStandardMaterial[]>([]);useFrame(({clock})=>caps.current.forEach((m,i)=>m.emissiveIntensity=.1+Math.max(0,Math.sin(clock.elapsedTime-i*.28))*.3));return <group>{STONES.map(s=><group key={s.index} position={s.position} rotation={s.rotation}><mesh position={[0,.48,0]}><boxGeometry args={[.18,.9,.24]}/><meshStandardMaterial color="#2a2a28" roughness={.9}/><Edges color="#30372b"/></mesh><mesh position={[0,.97,0]}><boxGeometry args={[.2,.08,.26]}/><meshStandardMaterial ref={e=>{if(e)caps.current[s.index]=e}} color="#b87333" emissive="#8b5a2b" metalness={.82}/></mesh></group>)}</group>}
