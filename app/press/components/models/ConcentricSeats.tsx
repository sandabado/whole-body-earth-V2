import { SEATING_RINGS } from "./great-hall-data";
export default function ConcentricSeats(){return <group>{SEATING_RINGS.map((r,ri)=><group key={ri}>{Array.from({length:r.seats},(_,i)=>{const a=i/r.seats*Math.PI*2,x=Math.cos(a)*r.radius,z=Math.sin(a)*r.radius;return <mesh key={i} position={[x,.16+r.height,z]} rotation={[0,-a,0]} castShadow><boxGeometry args={[.16,.22,.22]}/><meshStandardMaterial color="#2a2a28" roughness={.86}/></mesh>})}<mesh position={[0,.04+r.height,0]} rotation={[-Math.PI/2,0,0]}><ringGeometry args={[r.radius-.08,r.radius+.08,64]}/><meshStandardMaterial color="#22231e" roughness={.9}/></mesh></group>)}</group>}
